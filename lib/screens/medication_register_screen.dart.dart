import 'package:flutter/material.dart';

class MedicationRegisterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Cadastro de Medicamentos')),
      body: Center(
        child: Text('Tela de Cadastro de Medicamentos em Desenvolvimento'),
      ),
    );
  }
}
